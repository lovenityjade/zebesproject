# Pixel finale effects atlas

Generated with the built-in ImageGen tool on 2026-09-19 for the requested pixel-texture revision. Temporary AI-generated asset; retain the project's AI disclosure. No Nintendo artwork is embedded in this atlas.

Runtime asset: `Unreal/Content/Finale/PixelEffects-v2.png` (original RGBA output, 1254 × 1254). Alpha is preserved. The engine samples the 4 × 4 atlas directly on the native pixel grid, without bilinear filtering, and quantizes color channels to match the scene. No offline image alteration was performed.

Rows: Hyper plasma frames; falling fireballs; impact animation; stone, organic fragment, energy shard, ember. Production integration uses normalized cell coordinates, so the generated dimensions need not be divisible by four.

## Final generation prompt

Use case: stylized-concept. Asset type: a single production VFX sprite atlas for a 2D pixel-art game. SNES 16-bit era Super Metroid style, faithful crisp clustered pixels, with the richness and punch of a 2026 pixel-art remaster. Genuine transparent background alpha, no checkerboard, no backdrop. Square 1024x1024 atlas arranged in exactly 4 columns x 4 rows of 256x256 cells; art remains inside each cell with 20px clear padding; no labels, no grid lines. Treat each cell as enlarged 32x32 pixel art, every drawn pixel an aligned 8x8 square, no anti-aliasing or blurry bloom. Row 1: four successive animation frames of a horizontally elongated organic rainbow plasma projectile, traveling LEFT to RIGHT, brilliant pale ivory core, interleaved teal/magenta/gold/violet jagged edges and a trailing taper on the LEFT, pointed head RIGHT. Not straight parallel rainbow stripes, not rings, not a laser pointer, no weapon. Row 2: four successive frames of a compact fiery orange yellow falling meteor, diagonally down RIGHT, chunky irregular coal core in leading lower right, broken orange ember tail to upper left. Row 3: four consecutive explosion frames, bright small irregular burst, larger orange irregular burst, expanded dark orange fragmented burst, scattered cooling cinders; no circular rings. Row 4: four different tiny irregular fragment sprites centered separately: ochre stone fragment with dark shaded edge, rust-red organic fragment restrained not gory, pale turquoise energy shard, amber glowing cinder. Clean readable sprite shapes, limited curated color palette, intentional pixels, no random texture noise. All 16 cells separate, ready for in-engine animation, no characters or scenery, no lettering.
